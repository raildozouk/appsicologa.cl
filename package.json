{
  "name": "appsicologa-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.5",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "echo \"Use: pnpm -C apps/api dev   |   pnpm -C apps/web dev\"",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
