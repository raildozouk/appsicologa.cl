openapi: 3.1.0
info:
  title: appsicologa API
  version: 0.1.0
servers:
  - url: http://localhost:3001
paths:
  /v1/health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                required: [ok]
